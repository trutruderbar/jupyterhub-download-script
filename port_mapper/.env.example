# =============================================================================
# Port Mapper 環境變數配置範例
# =============================================================================
#
# 使用說明：
# 1. 複製此檔案為 .env: cp .env.example .env
# 2. 根據您的環境修改相關變數
#

# Port Mapper 服務綁定位址
PORT_MAPPER_BIND=0.0.0.0

# Port Mapper 服務埠號
PORT_MAPPER_PORT=32000

# 認證 API URL (用於驗證使用者身份)
PORT_MAPPER_AUTH_URL=http://your-auth-api:8000/command

# 認證 Bearer Token (若需要)
PORT_MAPPER_AUTH_BEARER=your-bearer-token-here

# Port Mapper 密鑰 (用於加密)
PORT_MAPPER_SECRET=please-change-me-to-strong-random-string

# JupyterHub Kubernetes Namespace
JHUB_NAMESPACE=jhub

# Kubectl 指令
KUBECTL_BIN="microk8s kubectl"

# Token 有效期限 (秒)
PORT_MAPPER_TOKEN_SECONDS=28800

# 代理超時時間 (秒)
PORT_MAPPER_PROXY_TIMEOUT=600

# Port Mapper 公開存取 URL
PORT_MAPPER_PUBLIC_BASE_URL=https://your-domain.example.com:32000

# 認證 Me API URL (用於取得使用者資訊)
PORT_MAPPER_AUTH_ME_URL=https://your-auth-api.example.com/api/auth/me
